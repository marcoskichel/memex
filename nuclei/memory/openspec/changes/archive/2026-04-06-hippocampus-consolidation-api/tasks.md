## Tasks

- [x] Add `consolidate(): Promise<void>` to `Memory` interface in `memory-types.ts`
- [x] Implement `consolidate()` in `MemoryImpl` delegating to `amygdala.run()` + `hippocampus.run()`
- [x] `MemoryConfig.hippocampusScheduleMs` passes through to hippocampus `scheduleMs` in factory
