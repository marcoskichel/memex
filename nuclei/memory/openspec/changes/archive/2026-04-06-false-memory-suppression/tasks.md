## Tasks

- [x] Add `PendingConsolidation` type and three review methods to `Memory` interface in `memory-types.ts`; add `pendingConsolidationTtlMs` to `MemoryConfig`
- [x] Implement pending consolidation map, event handler, review methods, and TTL purge in `MemoryImpl`
