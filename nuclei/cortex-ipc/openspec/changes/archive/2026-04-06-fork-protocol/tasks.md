# Tasks: fork-protocol

- [x] Add `ForkPayload` interface (`{ outputPath: string }`) to `protocol.ts`
- [x] Add `fork: ForkPayload` to `RequestPayloadMap`
- [x] Add `'fork'` to `REQUEST_TYPES` array
- [x] Export `ForkPayload` from `src/index.ts`
- [x] Build passes with no errors
