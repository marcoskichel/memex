## Tasks

- [x] Add `ConsolidatePayload` and `consolidate` to `RequestPayloadMap` in `protocol.ts`
- [x] Add `consolidate` to `REQUEST_TYPES` array in `protocol.ts`
- [x] Add `consolidate` case to `dispatch()` in `handlers.ts`
