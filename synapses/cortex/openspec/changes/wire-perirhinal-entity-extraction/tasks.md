## 1. Broadcast wiring

- [x] 1.1 Add `'perirhinal:extraction:end'` to `MEMORY_EVENT_NAMES` in `src/bin/cortex-core.ts`

## 2. Tests

- [x] 2.1 Add test: `perirhinal:extraction:end` event is broadcast to connected clients (mirror existing amygdala event broadcast test pattern)
