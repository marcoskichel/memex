## Tasks

- [x] Add `category?: string` to `GetContextPayload` in `protocol.ts`
- [x] Add `SESSION_MATCH_BOOST`, `CATEGORY_MATCH_BOOST`, `MAX_EFFECTIVE_SCORE` constants and `applyContextBoosts()` in `handlers.ts`; call after `mergeRecallResults`
