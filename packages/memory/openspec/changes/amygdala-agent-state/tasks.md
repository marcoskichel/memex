## Tasks

- [x] Add `setAgentState()` to `Memory` interface; re-export `AgentState`; add `agentState?` to `MemoryConfig` in `memory-types.ts`
- [x] Implement `setAgentState()` delegation in `MemoryImpl`; wire `agentState` through factory
