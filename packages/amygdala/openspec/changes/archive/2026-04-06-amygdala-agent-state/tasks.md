## Tasks

- [x] Add `AgentState` type and `buildSystemPrompt()` to `amygdala-schema.ts`
- [x] Add `agentState` to `AmygdalaConfig`, add `setAgentState()` method, and use dynamic system prompt in `amygdala-process.ts`
